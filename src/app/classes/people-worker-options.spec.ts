import { PeopleWorkerOptions } from './people-worker-options';

describe('PeopleWorkerOptions', () => {
  it('should create an instance', () => {
    expect(new PeopleWorkerOptions()).toBeTruthy();
  });
});
