import { PeopleLayerManager } from './people-layer-manager';

describe('PeopleLayerManager', () => {
  it('should create an instance', () => {
    expect(new PeopleLayerManager()).toBeTruthy();
  });
});
